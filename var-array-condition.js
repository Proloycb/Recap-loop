var bottleColor = "yellow";
var bottleQuantity = 1;
var isFull = false;

// array 

var items = ["bottle", "bokto", "protik", "bonna"];
var length = items.length;
items.push("siam");
items.pop();
items.shift();
items.unshift("Proloy");
console.log (items);
// var position= items.indexOf("bokto");
// console.log (position);

// condition 

if (items.length<4) {
    console.log ("bokto tumi beriye jaw");
}
else {
    console.log ("bokto vitore tako");
}